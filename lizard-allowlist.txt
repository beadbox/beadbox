# CCN allowlist for legacy-acceptable high-CCN functions.
# Functions listed here are exempt from the CCN < 15 gate.
#
# Format (one entry per line, after this header):
#   <relative-file-path>:<function-name>  # <reason>  # <super-approval-bead-id>
#
# Discipline:
#   1. Adding entries REQUIRES super approval. The commit that adds an entry
#      MUST include the phrase "ALLOWLIST ADDITION sanctioned by super in
#      bb-XXXX" so QA / arch review can audit the trail. (Process rule, not
#      script-enforced — scripts/check-ccn-allowlist.sh just reads the file.)
#   2. Removing entries (driving complexity DOWN) requires NO approval.
#      Encouraged. Refactor the function below CCN 15 then delete the line.
#
# Format-history note (bb-ystl.5): bb-ystl.2 shipped this file with a
# `<function>  # <file>:<bead>  # <reason>` ordering and a subtract-count
# parser. bb-ystl.5 reworked the parser (per the commented contract that
# bb-ystl.2 left here) to do exact `<file>:<function>` matching, so the
# entries below are reformatted accordingly. Same data, different layout.
#
# Consumed by: scripts/check-ccn-allowlist.sh + .husky/pre-push +
# .github/workflows/quality-gates.yml (complexity-full-repo job).

# Pre-existing violations carried over from bb-ystl.2 with super approval.
# Tracked for cleanup in bb-nfy6 (P2). Removing them once bb-nfy6 refactors
# both functions below CCN 15 needs no extra approval.
packages/server/src/handlers/diagnostics.ts:runDiagnostics  # CCN 21 — exhaustive 14-state probe matrix; cleanup tracked in bb-nfy6  # bb-ystl.2
packages/server/src/handlers/formulas.ts:collectRootNodes  # CCN 17 (FALSE POSITIVE per bb-chq9: lizard 1.22.1 TS parser bundles collectRootNodes + decrementDependentsOf + appendCycleStragglers into one span; real CCN is 2). Tracking upstream — 1.22.0 + 1.22.1 included TS-parser fixes (PRs #466/#467/#468) but NOT this bug class. PM-groomed action: track 2 release cycles before escalating to arrow-form workaround per bb-fe03.2/fe03.4 precedent.  # bb-ystl.2 / bb-chq9

# Pre-existing client-side violations exposed by bb-ystl.5's per-file
# --mode=changed scan when bb-y729 first touched these files. The full-repo
# --mode=full scan collapses them under module-level CCN budget and reports
# clean; the parser-mode discrepancy itself is tracked in bb-ej3u. BeadRow
# refactor + computeWaves + EpicTree cleanup tracked in bb-hw2t.
packages/client/src/components/bead-table.tsx:BeadRow  # CCN 57 — pre-existing 53 + bb-y729 checkbox column +4; refactor tracked in bb-hw2t  # bb-y729
packages/client/src/components/bead-table.tsx:computeWaves  # CCN 16 — pre-existing wave-grouping topo sort; refactor tracked in bb-hw2t  # bb-y729
packages/client/src/components/epic-tree.tsx:EpicTree  # CCN 19 — pre-existing god-component (drag/drop + epic + bead + backlog + archive sections); refactor tracked in bb-hw2t  # bb-y729

# Pre-existing .tsx violations surfaced via bb-nypf when changed-mode globbed
# the .tsx file directly. Hidden from full-mode CI by the lizard `-l typescript`
# .tsx-discovery gap (root cause tracked in bb-ej3u — eng3 diagnosis bumped
# this from "parser-mode discrepancy" to "CI gate is structurally broken for
# our file mix"). Cleanup of all three functions tracked in bb-hw2t (P2).
# Both anonymous-function violations in activity-page.tsx (CCN 17 + 18)
# share the same `(anonymous)` key under the parser's exact-match semantics,
# so a single entry covers both.
packages/client/src/components/activity-page.tsx:handleKeyDown  # CCN 51 — keyboard nav state machine; cleanup tracked in bb-hw2t  # bb-nypf
packages/client/src/components/activity-page.tsx:(anonymous)  # CCN 17 + 18 — render-tree IIFE branches; cleanup tracked in bb-hw2t  # bb-nypf
packages/client/src/lib/posthog-provider.tsx:(anonymous)  # CCN 21 — useEffect body covering posthog init + identify + gate-listener + tauri probe; refactor tracked in bb-hw2t  # bb-j04q

# Pre-existing violations exposed by bb-ej3u's .tsx-discovery fix
# (super-approved 2026-05-03: pre-existing CCN exposed by bb-ej3u
# .tsx-discovery fix; no new debt; matches bb-ystl.5 precedent).
# These functions have been over CCN 15 since QD.5 landed but were
# silently dropped by lizard's directory-recursive `-l typescript`
# .tsx-skip in --mode=full. The fix exposes them under the gate's
# real scope; this batch records the existing-state baseline so CI
# stays green during the transition. Per-function refactors will be
# filed as separate v0.25.x or v0.26 work and removed from this list
# as they land. Removing entries (driving complexity DOWN) needs no
# super approval per discipline rule 2.
packages/server/src/lib/change-detector.ts:_handlePollError  # CCN 17 — error classification + backoff + circuit-breaker; pre-existing  # bb-ej3u
packages/client/src/components/add-workspace-dialog.tsx:(anonymous)  # CCN 16 — dialog form-validation IIFE; pre-existing  # bb-ej3u
packages/client/src/components/pipeline-flow.tsx:PipelineFlow  # CCN 31 — pipeline render with stage filtering + bead grouping; pre-existing  # bb-ej3u
packages/client/src/components/help-fab.tsx:cliDisplayName  # CCN 16 — CLI command-name display switch; pre-existing  # bb-ej3u
packages/client/src/components/activity-feed.tsx:(anonymous)  # CCN 21 — feed render IIFE branches; pre-existing  # bb-ej3u
packages/client/src/components/formula-dag.tsx:detectParallelGroups  # CCN 21 — DAG topo-sort + parallel-group detection; pre-existing  # bb-ej3u
packages/client/src/components/formula-dag.tsx:StepNodeComponent  # CCN 18 — DAG step-node render with status branches; pre-existing  # bb-ej3u
packages/client/src/components/bead-scheduling-controls.tsx:SchedulingControls  # CCN 20 — due/defer/estimate controls panel; pre-existing  # bb-ej3u
packages/client/src/components/formula-tree.tsx:formatDescription  # CCN 109 — formula-tree god-render (refactor candidate); pre-existing  # bb-ej3u
packages/client/src/components/header.tsx:Header  # CCN 23 — NEWLY VISIBLE true positive, not a false one: removing the Formulas flag stopped the analyzer folding Header into BeadboxLogoLoading, so it is now scored on its own. The file's real branching went DOWN (58->56 tokens). Debt is real; beadbox-smx extracts the nav tab strip and removes this line.  # beadbox-01f.1
packages/client/src/components/header.tsx:BeadboxLogoLoading  # CCN 35 — animated boot-state header; pre-existing  # bb-ej3u
packages/client/src/components/header.tsx:(anonymous)  # CCN 20 — header IIFE for tab + workspace switcher; pre-existing  # bb-ej3u
packages/client/src/components/bead-detail-panel.tsx:(anonymous)  # CCN 27 — detail panel render IIFE branches; pre-existing  # bb-ej3u
packages/client/src/components/startup-gate.tsx:errorTitle  # CCN 21 — error-state title selector; pre-existing  # bb-ej3u
packages/client/src/components/formulas-view.tsx:formatVars  # CCN 44 — formula-view god-render (refactor candidate); pre-existing  # bb-ej3u
packages/client/src/components/settings-dialog.tsx:platformLabel  # CCN 33 — platform-name label switch; pre-existing  # bb-ej3u
packages/client/src/components/settings-dialog.tsx:SettingsDialog  # CCN 20 — settings dialog god-component; pre-existing  # bb-ej3u
packages/client/src/components/bead-expanded-view-modal.tsx:BeadExpandedViewModal  # CCN 41 — expanded-view modal render; pre-existing  # bb-ej3u
packages/client/src/components/bead-metadata-controls.tsx:MetadataControls  # CCN 18 — metadata controls panel; pre-existing  # bb-ej3u

# super-approved 2026-05-03: pre-existing CCN exposed by bb-pgb0.1
# dead-code deletion (lizard parser surfaced function previously hidden
# in collapsed block); no new debt; matches bb-ej3u precedent. Refactor
# tracked under bb-3gnz (god-component split, v0.26 line — too invasive
# for v0.25 active-line per super 2026-05-03).
packages/client/src/components/home-page.tsx:BeadsEpicsViewer  # CCN 94 — home-page god-component (1132 LOC); pre-existing, surfaced via bb-pgb0.1 deletion shrinking the file into lizard's parsable range  # bb-pgb0.1
packages/client/src/components/bead-detail-panel.tsx:BeadDetailPanel  # pre-existing (CCN 85 on main, never listed); #40 adds type-catalog props (88). Decomposition is not landing-sized  # beadbox-01f.13
packages/client/src/components/filter-bar.tsx:FilterBar  # pre-existing (CCN 19 on main, never listed); #40 lowers it to 18  # beadbox-01f.13
packages/server/src/handlers/workspaces.ts:updateServerConnection  # CCN 22 — connection edit with external-scaffold write/restore (PR #42); refactor proposed  # beadbox-01f.12
packages/server/src/lib/workspace-health.ts:resolvePort  # CCN 16 — registry-endpoint branch for external servers (PR #42); refactor proposed  # beadbox-01f.12
